<li class="control-group nested-fields">
  <div class="controls">
    <table width="500" >
      <tr>
        <td><%= f.collection_select :product_id, Product.order(:nombre), :id, :nombre, include_blank: true%></td>
        <td> <%= f.number_field :valor_unitario%>
          <input id="cantidad" name="catidad" type="number">
          <%= f.number_field :valor_total %>
          <%=link_to_remove_association "Eliminar", f %></td>
      </tr>
    </table>
  </div>
</li>
